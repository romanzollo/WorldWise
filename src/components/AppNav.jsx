import styles from './AppNav.module.css';

function AppNav() {
    return (
        <nav className={styles.nav}>
            App navigation
            <p>APP</p>
        </nav>
    );
}

export default AppNav;
